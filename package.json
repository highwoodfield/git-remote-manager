{
  "name": "git-remote-manager",
  "version": "0.1.0",
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "build": "npx tsc",
    "run": "npm run build && node main.js"
  },
  "bin":{
    "git-remote-manager": "main.js"
  }
}
